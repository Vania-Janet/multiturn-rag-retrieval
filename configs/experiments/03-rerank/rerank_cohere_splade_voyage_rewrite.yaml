# Reranking: SPLADE + Voyage-3 with Ground Truth Rewrites + Cohere Rerank-3.5
# For: CLAPNQ, GOVT (strongest performing domains)
# Expected: 0.53-0.56 nDCG@10 → Potential +2-5% with Cohere

experiment:
  name: "rerank_cohere_splade_voyage_rewrite"
  type: "retrieval"
  description: "Hybrid SPLADE + Voyage-3 with RRF fusion, ground truth rewrites, and Cohere reranking"

data:
  domains: ["clapnq", "govt"]
  query_mode: "rewrite"
  query_file: "data/retrieval_tasks/{domain}/{domain}_rewrite.jsonl"
  qrels_file: "data/retrieval_tasks/{domain}/qrels/dev.tsv"
  corpus_path: "data/passage_level_processed/{domain}"

retrieval:
  type: "hybrid"
  fusion_method: "rrf"  # Reciprocal Rank Fusion
  rrf_k: 60
  sparse:
    method: "splade"
    top_k: 300
  dense:
    method: "voyage"
    model_name: "voyage-3-large"
    top_k: 300

query_transform:
  enabled: false

reranking:
  enabled: true
  type: "cohere"
  model_name: "rerank-v4-pro"  # Cohere Rerank 4 Pro (latest, best performance)
  top_k_candidates: 100  # Rerank top 100 from fusion
  max_chunks_per_doc: 10

output:
  save_results: true
  results_dir: "experiments/{experiment_name}/{domain}"
  
metrics:
  compute: ["ndcg@5", "ndcg@10", "ndcg@20", "recall@5", "recall@10", "recall@20", "map@10", "precision@5"]
