experiment:
  name: "replication_elser"
  description: "Paper Replication: ELSER Baseline (Last Turn)"

data:
  query_mode: "last_turn"
  # Override domain config to use lastturn files
  query_file_suffix: "lastturn"

retrieval:
  type: "sparse"
  method: "elser"
  top_k: 300
