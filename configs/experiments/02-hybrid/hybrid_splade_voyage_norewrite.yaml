experiment:
  name: "hybrid_splade_voyage_norewrite"
  description: "Hybrid Fusion (SPLADE + Voyage) Rewrite OFF"

retrieval:
  type: "hybrid"
  alpha: 0.5
  sparse:
    method: "splade"
    index_path: "indices/clapnq/splade"
  dense:
    model_name: "voyage-large-2" # Will be overridden by run.py for fiqa
    index_path: "indices/clapnq/dense"
  top_k: 300

query_transform:
  enabled: false

data:
  query_mode: "last_turn"
