experiment:
  name: "hybrid_elser_bgem3_norewrite"
  description: "Hybrid Fusion (ELSER + BGE-M3) Rewrite OFF"

retrieval:
  type: "hybrid"
  alpha: 0.5
  sparse:
    method: "elser"
    index_path: "indices/clapnq/elser"
  dense:
    model_name: "BAAI/bge-m3"
    index_path: "indices/clapnq/dense"
  top_k: 300

query_transform:
  enabled: false

data:
  query_mode: "last_turn"
